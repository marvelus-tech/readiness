{
  "name": "readiness",
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "db:init": "wrangler d1 execute readiness-db --local --file=./schema.sql"
  },
  "devDependencies": {
    "wrangler": "^3.78.12"
  }
}
